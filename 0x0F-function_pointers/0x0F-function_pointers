#include "function_pointers.h"

/**
*print_name - prints names
*@name:name to be printed
*@f: function which to be printed names
*/

void print_name(char *name, void (*f)(char *))
{

if (name == NULL || f == NULL)
return;

f(name);

}
